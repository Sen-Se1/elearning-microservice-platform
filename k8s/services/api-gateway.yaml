# NodePort — main app entry point at http://192.168.66.10:30080
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: elearning
spec:
  selector:
    app: api-gateway
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080
  type: NodePort